#include<iostream>
using namespace std;

struct linked_list{
    int data ;
    struct linked_list *next;
    };
typedef struct linked_list node:
node*head;
head = (node*)malloc(size of (node)):
head->data=10;
head->next=NULL;
